import 'package:flutter/material.dart';

import '../../../../core/theme/styles.dart';

class XPayViewBody extends StatelessWidget {
  const XPayViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'X Pay View',
        style: Styles.textStyle30,
      ),
    );
  }
}
